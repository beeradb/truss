@AGENTS.md

<!--
The project's instructions live in AGENTS.md, which is the cross-tool open format
(agents.md). This file exists only because Claude Code reads CLAUDE.md and does not
read AGENTS.md, so the one-line import above is the documented bridge between the two.

Do not add content here. Anything written below the import is invisible to every
other agent tool, which is the whole reason the instructions live there.
-->
